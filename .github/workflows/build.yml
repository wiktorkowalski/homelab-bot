name: Build and Deploy

on:
  push:
    branches: [master]
  pull_request:
    branches: [master]

permissions:
  contents: read
  packages: write

jobs:
  build-deploy:
    uses: wiktorkowalski/github-workflows/.github/workflows/dotnet-docker-deploy.yml@master
    with:
      image-name: wiktorkowalski/homelab-bot
      dotnet-project: src/HomelabBot/HomelabBot.csproj
      deploy-compose-path: /home/ubuntu/docker/homelab-bot
      deploy-runner-labels: '["self-hosted", "homelab-bot"]'
    secrets: inherit
